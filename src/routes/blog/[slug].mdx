import { useParams } from "@solidjs/router";

export const route = { info: { enableScrollRestoration: false } };

<a href="/blog" class="inline-block mb-4 text-sm text-blue-600 hover:underline">&larr; Back to blog</a>

# Blog Post: {useParams().slug}

<div class="mt-8 rounded-lg bg-gray-50 p-6 text-sm text-gray-600">
  This page was prerendered at build time via SSG.
  The slug <code class="rounded bg-gray-200 px-1">{useParams().slug}</code>
  was specified in the prerender config.
</div>
